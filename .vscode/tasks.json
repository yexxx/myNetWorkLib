{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake .. && make -j16",
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ],
    "version": "2.0.0"
}